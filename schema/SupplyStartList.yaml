type: object
required:
- items
- count
properties:
  items:
    type: array
    items:
      $ref: ./SupplyStart.yaml
    description: List of supply start requests
    x-de: Liste der Lieferbeginn-Anfragen
  count:
    type: integer
    description: Total number of items (after filtering)
    x-de: Gesamtanzahl (nach Filter)
